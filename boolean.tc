mod bools {
    fn sc_test() {
        let a: bool = true, b: bool = false, c: bool = true;

        a &&= b;
        a ||= c;

        let d: bool = true;
        d &&= a || b && c;
    }

    fn condexpr_test() {
        let x: bool = true;
        let y: bool = false;

        let z: i32 = x ? (y ? 1 : 17) : 22;
    }
}
